method: bayes
metric:
  name: task4/val/acc_best
  goal: maximize
parameters:
  epochs:
    distribution: constant
    value: 500
  M:
    distribution: int_uniform
    min: 20
    max: 200
  lr:
    distribution: uniform
    min: 0.001
    max: 0.01
  batch_size:
    distribution: constant
    value: 512
  beta:
    distribution: uniform
    min: 1.0
    max: 10.0
  seed:
    distribution: constant
    value: 42
command:
  - ${env}
  - python3
  - /mnt/share/sanyam/src/continual_gp/experiments/mnist.py
  - s-mnist
  - --data-dir=/root
  - ${args}
