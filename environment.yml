name: cgp
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip
  - pip:
    - ipykernel
    - ipywidgets
    - seaborn
    - numpy
    - tqdm
    - fire
    - tensorboard
    - torch
    - torchvision
    # For experiment management only.
    - michelangelo-python-client
